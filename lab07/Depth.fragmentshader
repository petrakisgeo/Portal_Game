#version 330 core

// Output data
layout(location = 0) out float fragmentDepth;

void main()
{
    // Not really needed, OpenGL does it anyway
    fragmentDepth = gl_FragCoord.z; //apostasi sto lightspace thn grafei sto texture giauto einai out
}